import { ParseByTypePipe } from './parse-by-type.pipe';

describe('ParseByTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ParseByTypePipe();
    expect(pipe).toBeTruthy();
  });
});
